package edu.ib.newtoncalc;

public class StringModifier {
    /*public String modifyToEquation(String string){
        String[] parts = string.split(",");
        String plus = "+";
        for (int i = 0; i < parts.length; i++){
            if (Double.parseDouble(parts[i])>=0) {
                parts[i] = plus.concat(parts[i]);
            }
        }
        String result = "";
        for (int n = 0; n < parts.length; n++){
            result += parts[n]+"x^"+n;
        }
        return result.replace("+","%2B");
    }

    public double[] getCoefficients(String string){
        String[] parts = string.split(",");
        double[] doubleValues = new double[parts.length];
        for (int i = 0; i< parts.length; i++) doubleValues[i]=Double.parseDouble(parts[i]);
        return doubleValues;
    }*/
}
